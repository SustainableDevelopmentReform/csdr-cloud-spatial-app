services:
  db:
    platform: linux/x86_64
    image: postgis/postgis:17-master
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
      POSTGRES_DB: csdr-dev
    ports:
      - 5431:5432
